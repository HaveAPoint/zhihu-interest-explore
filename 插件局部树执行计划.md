# 插件局部树执行计划

日期：2026-09-14。范围：作者负责的插件局部树。状态：本轮只制定计划，以下任务未实施。

总计划见 [作者本人开发计划](作者本人开发计划.md)，产品规则见 [设计文档](docs/设计文档.md) 第 5、6、8、13 节；本计划细化 T19～T22，衔接 T13、T23、T33。分工及存储以 [分工说明](docs/分工说明.md) 的最新修正为准。

## 1. 目标和边界

在真实 Agent 尚未接入时，先完成可操作、可保存、可恢复的局部追问树：选中文字开树 → 查看短标题树 → 在回答中选区追问 → 分叉 → 折叠/展开 → 高亮箭头 → 长按拖删 → 重返恢复。

保留既有原生 DOM + TypeScript、MV3、IndexedDB 和整树同步。真实 Agent 的函数实现、模型密钥、OAuth、云数据库迁移、独立页布局、部署工具不是本轮实现范围。模型生成内容用可控 HTTP 假服务替代；正式插件保持调用已有业务接口，不能重新 import `buildMock*`。

本轮局部树包含挂靠状态的正确展示、现有挂靠入口接线和知识树入口；完整全局骨架选择器/个人节点编辑仍归 T23。有缺失必须单独记录，不能宣称整个产品已经完成。

当前工作区有其他部署工作未提交。执行前读取 `git status` 和相关 diff，保留已有改动；尤其 `background/index.ts` 是共享接线处，只修改本任务需要的消息字段，不整文件重写。

## 2. 当前代码与改造落点

| 当前文件 | 已有能力 | 本轮需要补齐 |
| --- | --- | --- |
| `plugin/src/content/overlay/overlay-view.ts` | 抽屉、学科选择、引文、按钮列表、单回答、普通 HTML 拖删 | 稳定 DOM、树状态、真正层级、答案链、长按手势、统一异步状态 |
| `plugin/src/content/index.ts` | CREATE_TREE/ADD_NODE/DELETE_NODE、刷新锚点、回跳接线 | 去掉 `(overlay as any).currentTree`；传准确来源与定位；处理迟到 UI 更新 |
| `plugin/src/content/selection.ts` | 文章选区、浮动按钮、Range | 严格限定正文；允许非空单字；准确重复文本偏移；可销毁监听 |
| `plugin/src/content/anchors.ts` | 包 mark、节点数、回跳 | 提供根 Range 查询；不把 badge 数字写回正文；不抢先消费深链接 |
| `plugin/src/background/tree-service.ts` | HTTP 生成、完整树、原子追加 | 仅补高亮定位字段透传、必要的稳定返回数据；保留原子追加 |
| `plugin/src/storage/indexeddb-repo.ts` | 整树与 outbox、`appendNodeAtomic` | 原则上不改；用测试证明 UI 没绕开原子写 |
| `packages/domain/src/local-tree.ts` | 树校验、删除后代 | 复用校验与后代集合，新增独立视图状态/布局纯函数 |
| `packages/contracts/src/tree.ts` | `highlight_anchor` 已存在 | 复用已有可选字段；必要时仅补请求透传，保持旧记录兼容 |
| `package.json` / `vitest.config.ts` | 单测与 API 测试 | `test:e2e` 仍是占位命令，需要真实浏览器测试入口 |

不要在修局部树时重写第一、二批认证/同步；发现明确回归，按独立修复单报告。

## 3. 先固定交互语义

### 3.1 短标题树与回答区分开

短标题树只显示标题、展开指示及折叠后代数量。回答区显示原文引用、本次高亮、用户问题、补充解释、参考来源。图上的父子线说明层级；回答区的箭头必须从实际高亮出发，不能拿节点框连线冒充高亮箭头。

新标题遵守已有 6～12 字生成约定；历史长标题只做展示截断/悬浮完整标题，不自动改存储值。新回答的「原文说的是」用该树 anchor_paragraph，父回答选区单独标注为「本次高亮」；旧数据的 answer_original 不批量重写，避免把历史记录当正文修正。

工程布局：顶部保留学科、挂靠、引文；下方是短标题树区域和回答区域。空间足够时并排；窄窗口时上下排列。局部树方向固定根左子右、兄弟上下，深度或分叉超宽时内部滚动，不能换成平铺标签。

回答的两种展示方式：

- 默认 `path`：从根到选中节点的回答链，最新回答是焦点；祖先回答保留为可定位的来源。
- `subtree`：双击某节点展开时，显示它及其后代的完整分叉回答区，按 parent_id 布局，不能擅自选一条子链丢掉兄弟分支。以该节点为起点；上游回答不在此视图时，不画伪造的上游箭头。

单条完整回答依然在回答区；树节点不重复塞入答案。

### 3.2 可见性与选择：精确定义

使用以下示例验证，字母只是测试别名，真实 ID 仍用稳定 UUID：

```text
A
├─ B
│  ├─ D
│  └─ F
└─ C
   └─ E
```

1. 重开树默认选中最后创建的回答；同时间按 ID 稳定排序兜底。示例最后创建 F，则焦点 F、路径 A/B/F。
2. 默认只展开焦点的祖先节点，允许看见这些祖先的直接孩子：示例可见 A/B/C/D/F，C 的后代 E 收起。这样既能看见分叉入口，又不铺开所有追问。
3. click(X)：立即选中 X，打开祖先以确保 X 可见，高亮 root→X，回答切 `path`。不折叠其他显式展开节点，不用定时器等待双击。
4. dblclick(X)：先自然执行 click，再切换 X 的后代。展开时将 X 及非叶后代加入展开集合，答案切 `subtree(X)`；收起时移除这些展开项，答案回到 root→X 的 `path`。叶节点双击仅维持选中，无折叠副作用。
5. 新回答成功：明确传回 `newNodeId`，选中它；展开集合重置为新节点祖先，收起其他分支的后续节点。不能靠“最后一个数组元素”猜新节点。
6. 删除选中节点或其祖先：选中最近仍存在的祖先；删除其他支不改变当前选择。删根清空视图并关闭蒙版。
7. 外部同步更新：选择仍存在就保留；不存在时按旧父链找到最近存活祖先。修剪失效展开项和选区，不自动展开无关分支。

### 3.3 UI 状态不进入同步 version

```ts
type LocalTreeViewState = {
  treeId: string | null;
  selectedNodeId: string | null;
  expandedNodeIds: Set<string>;
  answerMode: 'path' | 'subtree';
  subtreeRootId: string | null;
  pendingSelection: AnswerSelection | null;
  pendingOperation: { requestId: string; treeId: string; parentId: string | null } | null;
};
```

状态、滚动位置、拖拽状态在 content 内持有；选中、折叠、滚动、resize 不调用写接口、不增加 tree.version。每次成功追问/删除仍由已有后台业务服务负责 version 与落库。首版不新增“上次选中节点”的云端字段。

### 3.4 选区与定位

`AnswerSelection` 明确携带 treeId、parentNodeId、exact、prefix、suffix、start/end offset、捕获时的回答文本。它属于某个答案内容容器，不是整个 window 的随意选中文字。

- 可选内容：根来自专栏正文；追问来自某一回答的解释正文。标题、来源、输入框、两个不同回答之间的跨容器选区都无效。
- 非空单字有效；不要继续使用 `text.length > 1`。提问可空。输入法组合状态下 Enter 不提交。
- offset 使用浏览器文本节点的 UTF-16 下标，不能与 Unicode 字符计数混用。exact 与 DOM 文本必须一致；只用 trim 判断是否全空白，不在计算 offset 后随意裁剪文本。
- 用户选中第二个同名词，必须定位第二处；跨 `<strong>` 等文本节点仍正确。
- 点击输入框会使原生选区消失：在确认选区有效后保存定位描述，显示待追问高亮；聚焦输入框不清除此描述。
- 显式点击别处取消、选中新文本、切换回答/树、关闭蒙版、删除来源、来源文字改变时，清除旧待追问选区。发送前重新校验来源文本仍匹配。
- 已成功追问的高亮随 LocalNode 的 `highlight_anchor` 持久化。仅把 Range 留在内存不算完成。
- 旧节点无定位字段：exact 唯一匹配时恢复；多处匹配且上下文无法判定则不画错误高亮/箭头，仍允许读答案。兼容恢复不回写业务 version。

根高亮存入根 LocalNode.highlight_anchor；子节点的定位偏移相对 parent.answer_extra。不存 DOM 路径/屏幕坐标。已有字段足够时不增加数据库列。

### 3.5 高亮与箭头

使用真实文本 DOM + `<mark>`，样式前缀 `zhihu-explore-`。正文和解释均按文本渲染，禁止把 Agent 输出当 HTML。重叠高亮合并为不重叠的文本片段，每个片段记录所关联的 child IDs；同一句产生两条追问可共享高亮，箭头分别指向两个子回答，不嵌套一堆 mark。

箭头用独立、顶层 fixed SVG，pointer-events:none。所有端点统一用 viewport 坐标；不要叠加 scrollY 或手工乘浏览器缩放比例。多行高亮用实际 `Range.getClientRects()`，选择靠目标一侧的可见行框边缘，目标落在回答卡片入口。线为细实线、直线/折线、小箭头，避开文字区域。

端点不可见、被滚动容器裁掉、节点已收起或定位失败时隐藏该箭头；重新可见后重算。根箭头来自实际文章 Range；文章 Range 无法定位时不拿引文卡片假装原文起点。

窗口滚动、回答区滚动、resize、展开、文字换行、容器尺寸改变后，以一个 requestAnimationFrame 合并重算。隐藏/关闭蒙版立即停止绘制，解除监听；重开重新定位，不复用断开的 Range。

### 3.6 长按拖删

桌面 pointer 主键在短标题节点按住 400ms 才进入拖拽。400ms 内移动超过 6px，取消长按判定；普通点击/双击不触发删除。

进入拖拽后使用 pointer capture，显示 ghost 和固定底部删除栏。必须用 pointerup 的实际坐标检查是否落在删除栏矩形内；capture 后不能拿 event.target 作为命中依据。松在栏外、Esc、pointercancel、失焦、关闭蒙版均取消。拖拽结束抑制随后的合成 click/dblclick。

删除栏文案：「松手删除此节点及其后全部追问」。本轮蒙版内无二次确认。收到后台本地原子删除成功后移除节点；失败保留树并在蒙版内提示可重试，不用 alert 阻断页面。

### 3.7 异步更新和宿主页面

请求捕获 treeId、parentId、requestId 和当前 view session。生成中切到别棵树/关闭蒙版，结果仍可由后台保存，但不能重新打开旧树或覆盖新视图。前台不预造“成功节点”；后台返回 STALE_RESPONSE 时刷新该树状态，不能重新提交来复活数据。

同一蒙版首版只允许一个生成提交在途；切换节点和查看答案仍可用，输入保留。并发来自其他标签时依赖已有原子追加，本轮不引入另一个 version 计数器。

宽度方案是实现默认值，不是产品新规则：基础约 360px，分叉需要时增长；上限 `min(900px, viewportWidth - 480px)`，若该值不足 360px 则使用不超过视口宽度的覆盖布局。内部区域负责剩余滚动，页面始终有可达关闭按钮。检查 1280×800、1440×900、1920×1080 三种桌面尺寸。

使用单独宿主布局管理器保存原始内联样式值及 priority，关闭时还原；不能统一写 marginRight=0。只操作已识别的文章布局容器/右侧推荐，不删除知乎 DOM。若运行期间网站改动相同样式，仅还原仍等于插件写入值的字段，避免覆盖网站新状态。

## 4. 不依赖真实 Agent 的开发与验收环境

准备两层 fixture，不能混用验证结论：

1. **纯视图 fixture**：包含上述 A/B/C/D/E/F 树、长标题、多行解释、重复词、同高亮分叉、旧记录无 anchor、单根、深 8 层、宽 8 分支、100 节点。全部满足现有 schema；各案例用独立存储分区。
2. **本地假 HTTP 服务**：提供当前插件实际使用的 classify/root/followup 路由及同形响应，响应可手动释放/失败；生成 ID 沿用请求，正文固定，记录请求体供断言。只存在于 tests，生产无注入/控制路由。局部树主线不依赖云端数据库、OAuth 或队友 provider。

浏览器夹具加载实际编译后的 content/worker，使用隔离的 Chromium profile 与实际 IndexedDB。测试在网络路由层把一个专栏格式 URL 响应为本地文章 DOM fixture，避免访问真实文章；实际专栏仍有最后一次人工操作验收。不把该 fixture 作为公开仿专栏页面发布。

工具选择：沿用 Vitest 测纯函数，用 Playwright 自带 Chromium persistent context 测扩展。锁定兼容版本；测试页的网络拦截不作为 worker HTTP mock，worker 指向本地假 HTTP 服务。依据 [Playwright 扩展测试文档](https://playwright.dev/docs/chrome-extensions)。几何 API 依据 [Range.getClientRects](https://developer.mozilla.org/en-US/docs/Web/API/Range/getClientRects)、手势依据 [setPointerCapture](https://developer.mozilla.org/en-US/docs/Web/API/Element/setPointerCapture)。

生产构建不能包含 tests 页面、假 HTTP 服务、夹具树或“注入示例到用户存储”的入口。真实专栏人工测试如需假回答，使用隔离测试包明确标“本地测试回答”，保持正式包的接口配置不变。

## 5. 可逐项领取的任务卡

每卡约 30～90 分钟的单一目标；这是拆分粒度，不是工时保证。到时未收敛按卡内阶段继续拆，不扩大修改范围。完成后先跑本卡验收，再交我检阅。

### LT01：局部树夹具与测试入口

- 前置：无。范围：`tests/local-tree/fixtures/`、`tests/local-tree/mock-api.ts`、`playwright.local-tree.config.ts`、必要的 package/TS/Vitest 配置。
- 工作：准备第 4 节输入与可延迟 HTTP 服务；新增 `test:local-tree:unit`、`test:local-tree:browser`、`typecheck:local-tree-tests`。单测可先为空但命令不得伪造成功；有测试后要求实际执行数量非零。
- 验收：fixture 经 LocalTreeSchema 和 validateLocalTree 校验；真实编译扩展可在隔离浏览器加载；无真实 Agent/云凭据仍能返回假 root/followup。测试 TS 纳入类型检查。
- 禁区：不改正式 Agent、不发布、不覆盖现有 test:e2e 或验收脚本；保留其他人的部署改动。

### LT02：状态机和可见节点纯函数

- 前置：LT01。范围：`packages/domain/src/local-view-state.ts`、单测、必要 export。
- 工作：实现 OPEN/SELECT/TOGGLE_SUBTREE/NODE_ADDED/TREE_REPLACED/DELETE_APPLIED/CLOSE 转换；规范化 rootPath、childrenMap、最新节点、visibleNodeIds。先调用树校验，非法结构返回明确错误，不递归死循环。
- 验收：第 3.2 节七条规则逐条断言；A/B/C/D/E/F 具体可见集合正确；UI 动作不改变输入 tree、version 或节点内容。乱序输入仍确定排序。

### LT03：层级布局纯函数

- 前置：LT02。范围：`packages/domain/src/local-layout.ts`、布局测试。
- 工作：输入可见节点、父关系、节点测量尺寸和间距；按子树占用高度分配 y，depth 决定 x，父节点居中对齐直接孩子范围。输出节点矩形、折线转折点、内容总宽高；不能把坐标散写在 DOM 事件里。
- 验收：单链/宽分支/混合深度无矩形重叠，每条边连正确 parent；兄弟排序稳定；支持可变高的回答卡片尺寸。100 节点计算无无限循环。

### LT04：拆分蒙版并稳定 DOM 生命周期

- 前置：LT02。范围：`overlay/overlay-view.ts`，新增 `local-tree-view.ts`、`answer-view.ts`、`composer-view.ts`、`view-types.ts`；只拆这几个有职责的模块。
- 工作：OverlayView 变为编排者；节点/回答按 ID 保留 DOM，更新目标区域，避免每次 `container.innerHTML=''` 清掉输入、Range 和焦点。保留分类失败/重试/学科选择现有功能。
- 验收：输入一半后 updateArticle 不丢文本；更新别支不重建当前答案 DOM；重复开关 20 次仅一个容器，销毁后无监听/定时器残留。无 React 和 HTML 输出注入。

### LT05：接通树渲染、click/dblclick 与答案链

- 前置：LT03、LT04。范围：`overlay/local-tree-view.ts`、`answer-view.ts`、局部样式。
- 工作：用状态/布局输出代替 flex-wrap；绘制短标题节点和层级线；接 path/subtree 两种回答布局。click 不定时等待，且不能替换被点击的节点 DOM，否则浏览器可能无法识别随后的 dblclick；dblclick 操作幂等且顺序正确。选中路径用描边/线色，和全局点亮含义分开。
- 验收：A/B/F 路径默认展开、C 后代折起；单击 C 立即选中；双击 C 展示 C/E，双击收回；再双击 B 展示 B/D/F 两支。标题不显示整段问答。
- 交付节点 M1：本地 fixture 已能看到并操作真正的分叉树，立即给截图或可运行测试入口，不等箭头全部完成才展示效果。

### LT06：回答选区描述与高亮恢复

- 前置：LT04、LT05。范围：新增 `content/text-range.ts`、`answer-selection.ts`、`highlight-renderer.ts`，有限修改现有 selection.ts。
- 工作：实现容器内文本节点索引→Range/offset 双向映射、选区归属、待追问选区生命周期、重叠高亮分段；收集根选区，提供序列化字段。
- 验收：选中第二个重复词、跨 strong、多行、emoji、单字均定位正确；跨回答/正文外/输入框无请求；聚焦输入框保留描述，切树清除；来源文本更新后拒绝旧描述。

### LT07：定位字段透传与异步消息接线

- 前置：LT06。范围：`OverlayCallbacks`、content/index.ts、background/index.ts 对应 CREATE_TREE/ADD_NODE 分支、tree-service.ts 参数及节点构造、contracts 必要的可选请求字段。
- 工作：用结构化 FollowupIntent（treeId、parentId、highlight、anchor、question）替换三个裸参数；内容脚本不读 OverlayView 私有字段。把根/子 highlight_anchor 存入节点，保留已有 `appendNodeAtomic`；生成返回 newNodeId + 已保存快照或明确刷新结果。
- 重试细节：一次用户提交复用同一 requestId 和稳定 node ID；错误重试前先查询该操作是否已保存，已保存则显示现有结果。若现有消息/草稿无法关联操作，在此卡记录并补最小透传字段，不能仅靠按钮 disabled 假定 worker 重启/丢响应不会重复。需要改 repository 时只补操作幂等检查，保留原事务边界。
- 验收：页面捕获→消息→HTTP→实际本地节点的定位字段一致，读回后高亮可恢复；新回答聚焦它自己；关蒙版/切树后迟到结果不抢 UI；已删父节点仍返回 STALE_RESPONSE，不绕过原子追加。重试保留用户输入，不假装已成功。
- 禁区：不改 Agent 三函数签名，不把 DOM Range/屏幕坐标传进 HTTP/IndexedDB，不重设计同步协议。

### LT08：高亮到回答的 SVG 箭头

- 前置：LT03、LT05、LT06、LT07。范围：新增 `content/arrow-layer.ts`、几何单测与浏览器测试。
- 工作：独立管理 viewport SVG、所有可见边的起止 Range/Rect、重算调度、clip/隐藏与销毁；同时支持文章根→根回答和父回答高亮→子回答。
- 验收：截图不能代替端点断言：测量结果与箭头起终点的偏差不超过 4 CSS px；滚动正文/答案、调整窗口、折叠、同高亮双分支均正确。节点或来源不可见就没有悬空旧箭头。

### LT09：长按拖删手势

- 前置：LT05、LT07。范围：新增 `content/node-drag.ts`，替换 overlay 原生 draggable 处理。
- 工作：400ms 阈值、6px 抖动取消、pointer capture、ghost、底部栏、pointerup 命中、取消路径、单次删除锁与合成 click 抑制。
- 验收：399ms 松手不拖删；按住 400ms 后栏出现；栏外/Esc/cancel 不删；栏内一次 pointerup 只发一次 DELETE_NODE；失败树仍可见；删 B 只删 B/D/F，删 A 关闭蒙版。

### LT10：宽度、宿主布局与原文锚点协调

- 前置：LT05、LT08、LT09。范围：新增 `content/host-layout.ts`，有限修改 anchors.ts、zhihu-adapter.ts、overlay 容器样式。
- 工作：按第 3.7 节计算宽度及内部滚动；保存/还原宿主样式；暴露根 anchor Range 查询；锚点清理时去掉自己插入的 badge 再还原原文，重叠标记按原始文本统一重建。
- 验收：三种桌面尺寸可读；分叉变宽、收起回收；关闭后正文文本逐字不变、网站原 margin/transition/推荐栏状态恢复；节点数不进入正文；事件回调先注册再恢复深链接。

### LT11：删除、外部更新和旧数据的整体协调

- 前置：LT07～LT10。范围：Overlay 编排、content/index.ts 事件接线、对应浏览器测试。
- 工作：删除选中祖先后的回退、另一支完成不抢当前操作、同步替换失效节点处理、来源变化时清选区、关闭/切换释放图层；旧节点无 anchor 的保守定位。
- 验收：生成中删父/删根、快速切树、后台新版本替换、失败后重试、旧数据多处 exact 等场景，无旧树闪回/悬空线/跨树选区。view-only 操作前后全树 JSON/hash/version 相同。

### LT12：挂靠状态、编辑标题与独立页入口接线

- 前置：LT05、LT07、LT11。范围：overlay 状态栏/现有详情入口、必要的 message callback；不写新的全局树编辑器。
- 工作：已绑/待绑文案读取真实 tree；追问不改变绑定；根无候选仍允许继续追问，接到 T23 骨架选择入口。详情提供已有短标题修改动作；增加 APP_ORIGIN 知识树入口，使用正确 discipline/node 参数。
- 验收：exact/非 exact/零候选夹具都能显示；改标题保留 highlight/答案/parent；view 操作不新增全局点亮；网页入口可达。若 T23 入口实际缺失，明确标为产品集成待办，不用无效按钮掩盖。

### LT13：浏览器回归、打包检查与最终检阅

- 前置：LT01～LT12。范围：`tests/local-tree/*.spec.ts`、`worklogs/LT-review.md`、`artifacts/local-tree/`。
- 工作：执行第 6 节矩阵，跑既有单测/接口测试回归；构建安装包确认测试资源未混入。保留 A/B/C/D/E/F 过程截图和短录屏，随后在真专栏用测试 HTTP provider 走一次。
- 验收：纯函数/浏览器/类型检查分别有实际命令与结果；真专栏操作单独写验证环境。结果不得写“真实 Agent 已验收”。我最后按源文件、交互证据和数据版本复核；P0 数据损坏/P1 核心交互失败未清零不得结项。

## 6. 验收矩阵

| 编号 | 操作/场景 | 必须观察到的结果 |
| --- | --- | --- |
| V01 | 打开单根与六节点树 | 短标题层级正确；最新节点与祖先链可见 |
| V02 | 单击，再双击父节点 | 点击立即响应；后代切换而非延迟模拟双击；兄弟分叉保留 |
| V03 | 双击叶节点 | 无异常，无节点/版本变化 |
| V04 | 从 A 两处高亮分别追问 B/C，再从 B 追问 D | parent_id 唯一正确，默认收起其他后续分支 |
| V05 | 无高亮、单字、跨回答选区、输入法 Enter | 无选区不发送；单字可发送；跨容器/组合输入不误提交 |
| V06 | 重复词第二处、跨标签、多行、重叠选区 | 高亮与箭头来源是实际选择，不总落第一处 |
| V07 | 原文/蒙版滚动、resize、展开来源、长回答换行 | 箭头端点偏差≤4 CSS px；不可见边隐藏 |
| V08 | 长按阈值内/外、栏内/外松手、取消 | 只有进入拖拽且栏内释放才删，一次动作一个请求 |
| V09 | 删 B / 删 A / 删除失败 | B/D/F 一起删而 C/E 保留；删根关树；失败可重试 |
| V10 | 生成期间删父/删根/切树/关蒙版 | 不复活、不抢焦点、不重开、不插孤儿节点 |
| V11 | 生成失败再试 | 输入和选区可恢复，不产生两个成功节点 |
| V12 | 刷新文章，或点击 explore_tree 回跳 | 已完成高亮/计数恢复；普通重访不弹；回跳开正确树 |
| V13 | UI 折叠/选中/滚动 20 次 | 同步 outbox 无新业务写，全树 version/hash 不变 |
| V14 | 开关蒙版 20 次，切换文章/树 | 无重复监听、孤立 DOM、残留定时器；正文逐字一致 |
| V15 | 旧数据缺定位、新数据刷新恢复 | 唯一 exact 恢复，多义不误连，答案始终可读 |
| V16 | 后台替换为删了当前节点的快照 | 回退最近存活祖先，选区/箭头不引用已删 DOM |
| V17 | 1280/1440/1920 桌面宽度，深 8 层/宽 8 支 | 可见内容不重叠，可滚到目标，原文/关闭入口可达 |
| V18 | 挂靠状态、标题编辑、知识树入口 | 全局只绑定根；标题编辑不改内容；入口指向真实独立页 |

对非叶布局、答案高亮、删除三类，不接受仅“页面没报错”的验收。每项必须检查 DOM 状态或实际保存数据；截图只证明视觉，不能证明事务或消息正确。

## 7. 执行顺序、阶段交付与检阅

```text
LT01 → LT02 → LT03 → LT04 → LT05 → LT06 → LT07
→ LT08 → LT09 → LT10 → LT11 → LT12 → LT13
```

- M1（LT05）：能看见真正的短标题分叉树，点击/双击/答案链可用。我检查状态语义，立即给用户看局部效果。
- M2（LT08）：选区、持久高亮、定位透传、箭头形成闭环。我检查重复词与坐标，不等真实 Agent。
- M3（LT11）：长按删除、原文挤压/恢复、异步更新都可用。我检查 DOM 生命周期和原子保存回归。
- M4（LT13）：接到已有插件，真专栏可操作，有证据，局部树任务通过。真实 Agent、T23 全局编辑器及全产品发布仍单列验收。

截止仍为 2026-09-15 10:00（北京时间）；执行者先读当前时间。优先完成上述里程碑，不先花时间在装饰动画、自动居中动效或扩展新功能。若时间不足，明确未完成任务，不能把平铺节点截图当完整树交付。

目前根 npm 的 `test:e2e` 占位命令不代表浏览器验收。LT01 新命令就绪后执行：

```sh
npm run typecheck
npm run typecheck:local-tree-tests
npm run test:local-tree:unit
npm run test:local-tree:browser
npm run test:unit
npm run test:integration
npm run build -w plugin
```

这些脚本中新增部分由 LT01 实现；本轮没有运行它们或修改业务代码。发布云端/替换正式下载包不属于本计划制定动作，按后续部署请求执行。

## 8. 给执行模型的领取模板

```text
读取《插件局部树执行计划.md》，本次只实施 LTxx。
先核对 git diff、依赖产物与任务卡；保留其他人的部署和认证/同步改动。
按已定状态机实现，不改产品规则，不依赖真实 Agent。
完成后交付：修改文件、实际验收命令/结果、覆盖 Vxx、截图或录屏位置、未完成项。
协议缺口必须给出具体调用链，不能用 any、空测试、假成功或新增 mock 分支绕过。
交给主检阅者检查后再进入下一里程碑。
```

计划自检：13 张任务卡覆盖状态、布局、选区、定位、箭头、删除、生命周期、异步更新、接口接线与浏览器验收；无真实 Agent 前置条件。原三份文档和总计划不改。
